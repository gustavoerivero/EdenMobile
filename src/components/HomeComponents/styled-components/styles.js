import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  imageBackground: {
    opacity: 1,
    borderRadius: 7,
    minHeight: 160,
  },
  gradient: {
    flex: 1,
    borderRadius: 7,
    minHeight: 160,
  }
})

export default styles